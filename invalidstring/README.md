# invalidstring

*just for testing invalid string data*

This crate is part of the [rusted-git](https://github.com/tiagoyamashita/rusted-git) project. We need this to be a separate crate so that `asyncgit` can remain forbidding `unsafe`.